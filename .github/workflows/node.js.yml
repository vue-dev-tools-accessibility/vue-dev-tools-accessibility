name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7
    - uses: volta-cli/action@v5
    - run: node -v
    - run: npm -v
    - run: npm ci
    - run: npm run lint
    - run: npm run build
    - run: npm t
